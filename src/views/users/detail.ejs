<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('../partial/head.ejs') %>
    </head>
    <body>
        <%- include('../partial/navbar.ejs') %>
        <div>
            <div>
                <img
                    class="h-32 w-full object-cover lg:h-48"
                    src="https://images.unsplash.com/photo-1444628838545-ac4016a5418a?ixid=MXwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHw%3D&ixlib=rb-1.2.1&auto=format&fit=crop&w=1950&q=80"
                    alt=""
                />
            </div>
            <div class="mx-auto max-w-5xl px-4 sm:px-6 lg:px-8">
                <div class="-mt-12 sm:-mt-16 sm:flex sm:items-end sm:space-x-5">
                    <div class="flex">
                        <img
                            class="h-24 w-24 rounded-full ring-4 ring-white sm:h-32 sm:w-32"
                            src="https://ui-avatars.com/api/?name=<%=account.name%>&background=0D8ABC&color=fff&size=32"
                            alt=""
                        />
                    </div>
                    <div class="mt-6 sm:flex sm:min-w-0 sm:flex-1 sm:items-center sm:justify-end sm:space-x-6 sm:pb-1">
                        <div class="mt-6 min-w-0 flex-1 sm:hidden 2xl:block">
                            <h1 class="truncate text-2xl font-bold text-gray-900"><%=account.name%></h1>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Tabs -->

        <!-- Description list -->
        <div class="mx-auto mt-6 max-w-5xl px-4 sm:px-6 lg:px-8">
            <dl class="flex flex-col gap-x-4 gap-y-8 sm:grid-cols-2">
                <div class="sm:col-span-1">
                    <dt class="text-sm font-medium text-gray-900">Username</dt>
                    <dd class="mt-1 text-sm text-gray-900"><%=account.username%></dd>
                </div>

                <div class="sm:col-span-1">
                    <dt class="text-sm font-medium text-gray-900">Date of Birth</dt>
                    <dd class="mt-1 text-sm text-gray-900"><%=account.yob%></dd>
                </div>

                <div class="sm:col-span-1">
                    <dt class="text-sm font-medium text-gray-900">Admin</dt>
                    <dd class="mt-1 text-sm text-gray-900"><%= account.isAdmin ? "Yes" : "Nope" %></dd>
                </div>
            </dl>
        </div>
    </body>
</html>
